CREATE TABLE IF NOT EXISTS words
(
    id              VARCHAR(60) NOT NULL,
    wordJapan       VARCHAR(60) NOT NULL,
    translateWord   VARCHAR(60) NOT NULL,
    levelN          VARCHAR(60) NOT NULL
    );